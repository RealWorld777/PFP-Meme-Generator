import Layout from "../layout";
import "../globals.css"
const About: React.FC = () => {
    return (
        <Layout >

            <div>
                <h1>About Page</h1>
                <p>This is the about page.</p>
            </div>
        </Layout>
    );
};

export default About;
